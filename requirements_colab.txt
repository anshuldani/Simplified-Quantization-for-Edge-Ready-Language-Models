# requirements_colab.txt
# Dependencies for Google Colab.
# Colab already ships torch + torchvision with CUDA, so we skip those.

# HuggingFace stack
transformers>=4.40.0
accelerate>=0.27.0
datasets>=2.18.0

# Evaluation
lm-eval>=0.4.2

# Numerics & data
numpy>=1.24.0
scipy>=1.11.0
pandas>=2.0.0
einops>=0.7.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0
rich>=13.7.0
psutil>=5.9.0
pytest>=7.4.0
wandb>=0.16.0

# Tokenizer
sentencepiece>=0.1.99
protobuf>=3.20.0

# GPTQ baseline (optional)
auto-gptq>=0.7.1
